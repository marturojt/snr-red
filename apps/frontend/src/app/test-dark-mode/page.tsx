import DarkModeTestComponent from '@/components/DarkModeTestComponent';

export default function TestPage() {
  return (
    <div className="min-h-screen bg-background">
      <DarkModeTestComponent />
    </div>
  );
}
